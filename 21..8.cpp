/*
Тут немає проблеми з пріоритетами операторів, 
оскільки оператор operator== — це окрема функція, 
де всі логічні оператори (==, &&, !) використовуються всередині 
виразів без змішування з потоковими операторами (<<).

Оператор operator== повертає bool, 
який вже можна безпомилково використовувати у будь-якому виразі,
 зокрема в умовах та у виводі.
 */