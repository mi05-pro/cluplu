/*
#include <string>

class Cat {
public:
    Cat(string name = "") : name(name) {}
    bool operator==(const Cat& other) const {
        return name == other.name;
    }
private:
    string name;
};

List<string> stringList;  // список рядків (типу string / Strings)
List<Cat> catList;        // список об'єктів типу Cat
List<int> intList;        // список цілих чисел

*/
